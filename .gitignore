target/
__pycache__/
*.so
*.pyd
*.pyc
.venv/
venv/
dist/
build/
*.egg-info/
.maturin/
.pytest_cache/
.mypy_cache/
.ruff_cache/
Cargo.lock

# Local-only reference material (PDFs, etc.) — not tracked.
refs/
*.pdf

# Benchmark output (Phase 10) — only baseline.json is committed.
# Run JSONs (incl. Phase 18 tune-*.json sweeps) and intermediate
# folded-stack dumps stay local. One flamegraph SVG is kept: the
# phase-of-record capture referenced by HOTSPOTS.md.
benches/results/*.json
!benches/results/baseline.json
benches/results/flamegraph-*.folded.txt
benches/results/flamegraph-*.svg
!benches/results/flamegraph-*-69e2053.svg
benches/results/PHASE14_NOTES.md
target/criterion/

# Workflow artifacts — never committed (Phase 18 hygiene).
subagents/
/tmp/
/prompts/
/NEXT_SESSION.md
/HANDOFF.md
/phase*-summary.md
/phase*-report.md
perf_review_*/
review_*/

# Bench-tier local cache (Phase 16) — per-developer quick/perf baselines.
.hexo/

# Promotion harness (Phase 11) — worktree of the locked best engine.
.worktree-best/

# ignore data (includes analysis output under data/analysis/)
data/

# local-only analysis scripts (not part of the engine)
scripts/analyze_human_games.py
scripts/openbook/
scripts/build_opening_book.py
