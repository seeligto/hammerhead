use crate::board::Board;
use crate::coords::Coord;
use smallvec::SmallVec;

pub fn order_moves(
    _board: &Board,
    _moves: &mut SmallVec<[Coord; 64]>,
    _tt_best: Option<Coord>,
    _depth: i8,
) {
    todo!()
}
