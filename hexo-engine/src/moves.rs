#![allow(clippy::must_use_candidate)]

use crate::board::Board;
use crate::coords::Coord;
use smallvec::SmallVec;

pub fn generate(_board: &Board, _radius: i16) -> SmallVec<[Coord; 64]> {
    todo!()
}
